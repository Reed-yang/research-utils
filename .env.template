# Root-level .env — shared across all skills.
# Copy this file to .env and fill in your keys.
#
# Priority: root .env > skill-local .env
# You can also keep per-skill .env files; any key not set in root
# will fall back to the local one.

# --- DeepSeek (paper-translate, subtitle-translate) ---
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# --- TensorBlock Forge (paper-translate, subtitle-translate) ---
TENSORBLOCK_API_KEY=
TENSORBLOCK_BASE_URL=https://api.forge.tensorblock.co/v1

# --- Zhipu AI / GLM-OCR (paper-ingestion) ---
GLM_API_ID=
GLM_API_KEY=
